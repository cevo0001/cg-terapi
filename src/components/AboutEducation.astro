<section class="education-section">
  <img class="education-image" src="/images/education.webp" alt="" />

  <div class="education-content">
    <h2>Mine uddannelser:</h2>

    <div class="education-grid">
      <div>Uddannelse</div>
      <div>Uddannelse</div>
      <div>Uddannelse</div>
      <div>Uddannelse</div>
    </div>

    <p>
      Jeg arbejder bl.a. med samtaleterapi, kropsterapi, nervesystemsregulering,
      intuitiv vejledning, energiarbejde og mental træning.
    </p>
  </div>
</section>

<style>
  .education-section {
    padding: 50px 24px;
    background: var(--sand);
    border-bottom: 1px solid var(--brown);
    display: grid;
    gap: 32px;
  }

  .education-image {
    width: 100%;
    max-width: 320px;
    margin: 0 auto;
  }

  .education-content h2 {
    font-size: 32px;
    margin-bottom: 24px;
    text-align: center;
  }

  .education-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    max-width: 360px;
    margin: 0 auto 28px;
  }

  .education-grid div {
    min-height: 110px;
    border: 1px solid var(--brown);
    border-radius: 10px;
    background: var(--dark-green);
    color: var(--cream);
    display: flex;
    align-items: center;
    justify-content: center;
    text-transform: uppercase;
  }

  .education-grid div:nth-child(even) {
    background: var(--light-green);
  }

  .education-content p {
    font-size: 16px;
    line-height: 1.7;
    text-align: center;
  }

  @media (min-width: 900px) {
    .education-section {
      grid-template-columns: 360px 1fr;
      align-items: center;
      padding: 60px 80px;
      gap: 80px;
    }

    .education-content h2 {
      text-align: left;
    }

    .education-grid {
      grid-template-columns: repeat(4, 1fr);
      max-width: 650px;
      margin: 0 0 28px;
    }

    .education-grid div {
      min-height: 140px;
    }

    .education-content p {
      text-align: left;
    }
  }
</style>