<section class="about-hero">

  <div class="about-inner">

    <div class="about-text">

      <h1>Jeg hedder Charlotte Grøngaard</h1>

      <div class="line"></div>

    </div>

    <div class="about-image">

      <img
        src="/images/ch2.webp"
        alt="Charlotte Grøngaard"
      />

    </div>

    <div class="about-description">

      <p>
        Mine behandlinger er skabt til at støtte dig i at finde mere ro, styrke og nærvær i din hverdag.
      </p>

      <img
        class="doodle"
        src="/images/flower-small.svg"
        alt=""
      />

    </div>

  </div>

</section>

<style>
.about-hero {
  background: var(--light-green);
  border-bottom: 1px solid var(--brown);
  padding: 0;
  
}

.about-inner {
  background: var(--light-green);
  max-width: 850px;
  margin: 0 auto;
  padding: 25px 20px 25px;
  text-align: center;
  border: 1px solid var(--brown);
}

.about-text {
  max-width: 500px;
  margin: 0 auto;

}

.about-text h1 {
  font-size: 30px;
  line-height: 1.05;
  margin-bottom: 12px;
}

.line {
  width: 55px;
  height: 0.5px;
  background: var(--brown);
  margin: 0 auto 18px;
}

.about-image img {
  width: 100%;
  max-width: 115px;
  aspect-ratio: 4 / 5;
  object-fit: cover;
  margin: 0 auto 18px;
  border: 1px solid var(--brown);
}

.about-description {
  max-width: 500px;
  margin: 0 auto;
}

.about-description p {
  font-size: 17px;
  line-height: 1.6;
  font-style: italic;
  max-width: 430px;
  font-weight: 400;
  margin: 0 auto;
}

.doodle {
  width: 65px;
  margin-top: 16px;
  opacity: 0.55;
}

@media (min-width: 900px) {
  .about-hero {
    padding: 20px 30px 20px;
  }

  .about-image img {
    max-width: 135px;
    margin-bottom: 20px;
  }

  .about-text h1 {
    font-size: 42px;
  }

  .about-description p {
    font-size: 17px;
    max-width: 470px;
    margin-bottom: -20px;
  }

  .doodle {
    width: 75px;
    margin-top: 18px;
  }

  .about-inner {
  background: var(--beige);
  max-width: 850px;
  margin: 0 auto;
  padding: 25px 20px 25px;
  text-align: center;
  border: 1px solid var(--brown);
}
}
</style>