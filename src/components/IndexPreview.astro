<section class="preview-section">

  <div class="preview-image">
    <img src="/images/ch1.webp" alt="">
  </div>

  <div class="preview-content">

    <h2>Jeg hedder Charlotte Grøngaard</h2>

    <p>
     Jeg arbejder med terapi, kropsbevidsthed og personlig udvikling med fokus på nærvær, balance og indre ro.
Hos mig mødes du i et trygt og nærværende rum med plads til refleksion, udvikling og en dybere kontakt til dig selv.
    </p>

    <a href="/om-mig" class="preview-button">
      Læs mere om mig
    </a>

  </div>

  <img class="flower-big" src="/images/flower-big.svg" alt="">
  <img class="flower-small" src="/images/flower-small.svg" alt="">

</section>

<style>

  .preview-section {
    position: relative;

    padding: 50px 22px;

    background: var(--beige);

    border-bottom: 1px solid var(--brown);

    display: grid;
    gap: 28px;

    overflow: hidden;

    text-align: center;
  }

  .preview-image img {
    width: 100%;
    max-width: 190px;

    margin: 0 auto;

    border: 1px solid var(--brown);

    display: block;
  }

  .preview-content {
    max-width: 320px;
    margin: 0 auto;
  }

  .preview-content h2 {
    font-size: 30px;

    line-height: 1.2;

    margin-bottom: 18px;
  }

  .preview-content p {
    font-size: 16px;

    line-height: 1.7;

    margin-bottom: 34px;
  }

  .preview-button {
    display: inline-block;
  border: 1px solid var(--brown);
    padding: 13px 26px;

    border-radius: 999px;

    background: var(--dark-green);
    color: var(--white);
    text-decoration: none;

    font-family: 'Source Serif Pro', serif;

    transition: 0.3s;
  }

  .preview-button:hover {
    background: var(--sand);
  }

  .flower-big {
    position: absolute;

    right: -70px;
    top: 20px;

    width: 180px;

    opacity: 0.18;
  }

  .flower-small {
    position: absolute;

    right: 28px;
    bottom: 24px;

    width: 70px;

    opacity: 0.7;
  }

  @media (min-width: 900px) {

    .preview-section {

      grid-template-columns: 300px 1fr;

      align-items: center;

      text-align: left;

      padding: 40px;

      gap: 90px;
    }

    .preview-image img {
      max-width: 220px;
      margin: 0;
      margin-left: 62px;
    }

    .preview-content {
      max-width: 700px;

      margin: 0;
    }

    .preview-content h2 {
      font-size: 30px;
    }

    .preview-button {
      font-size: 17px;
    }

    .preview-content p {
      font-size: 17px;

      max-width: 600px;

      margin-bottom: 50px;
    }

    .flower-big {
      right: -40px;
      top: 40px;

      width: 260px;
    }

    .flower-small {
      right: 260px;
      bottom: 40px;

      width: 90px;
    }

  }

</style>