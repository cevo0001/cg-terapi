<section class="contact-hero">

  <div class="hero-text">

    <h1>
      Book din tid eller send mig et spørgsmål
    </h1>

    <div class="line"></div>

    <p>
      Du er altid velkommen til at kontakte mig, hvis du har spørgsmål eller ønsker at booke en behandling.
    </p>

    <img
      class="fugl"
      src="/images/kontaktherofugl.png"
      alt=""
    />

  </div>

  <div class="hero-image">

    <img
      src="/images/cg5.webp"
      alt=""
    />

  </div>

</section>

<style>

  .contact-hero {
    display: grid;
    background: var(--beige);
    border-bottom: 1px solid var(--brown);
  }

  .hero-text {
    padding: 40px 24px;
  }

  .hero-text h1 {
    font-size: 35px;
    line-height: 1.15;
    margin-bottom: 16px;
    max-width: 480px;
  }

 

  .line {
    width: 80px;
    height: 0.5px;
    background: var(--brown);
    margin-bottom: 18px;
  }

  .hero-text p {
    max-width: 330px;
    line-height: 1.6;
    font-style: italic;
    font-weight: 400;
  }

  .fugl {
    width: 80px;
    margin-top: 20px;
    opacity: 0.6;
  }

  .hero-image img {
    width: 100%;
    height: 260px;
    object-fit: cover;
    margin-top: -60px;
  }

  @media (min-width: 900px) {
    .contact-hero {
      grid-template-columns: 0.9fr 1.1fr;
      min-height: 360px;
    }

    .hero-text {
      padding: 50px 95px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .hero-text h1 {
      font-size: 42px;
    }

    .hero-image img {
      height: 440px;
      width: 500px;
      margin-left: 195px;
      margin-top: 0px;
      border-left: 1px solid var(--brown);
    }

    .fugl {
      width: 4000px;
      margin-top: 10px;
      opacity: 1;
    }
    
  }



</style>