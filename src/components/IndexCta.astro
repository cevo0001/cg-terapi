<section class="cta-section">
  <div class="overlay">

    <div class="cta-content">
      <div class="cta-text">
        <h2>Klar til at tage første skridt?</h2>

        <p>
          Jeg er her for at støtte dig på rejsen
        </p>
      </div>

      <a href="/kontakt" class="cta-button">
        Book din tid
      </a>
    </div>

  </div>
</section>

<style>
  .cta-section {
    background-image: url('/images/cta-siv.webp');
    background-size: cover;
    background-position: center;

    border-bottom: 1px solid var(--brown);
  }

  .overlay {
    background: rgba(0, 0, 0, 0.16);

    padding: 55px 22px;
  }

  .cta-content {
    display: flex;
    flex-direction: column;

    align-items: center;
    text-align: center;

    gap: 26px;

    max-width: 320px;
    margin: 0 auto;
  }

  .cta-text h2 {
    color: var(--cream);

    font-size: 30px;
    font-weight: 400;
    line-height: 1.15;

    margin-bottom: 12px;
  }

  .cta-text p {
    color: var(--cream);
    font-weight: 400;
    font-size: 17px;

    line-height: 1.5;
  }

  .cta-button {
    width: fit-content;

    padding: 13px 28px;

    border-radius: 999px;
    border: 1px solid var(--brown);

    background: var(--cream);
    color: var(--brown);

    text-decoration: none;

    font-family: 'Source Serif Pro', serif;

    font-size: 17px;

    transition: 0.3s;
  }

  .cta-button:hover {
    background: var(--dark-green);
    color: var(--white);
  }

  @media (min-width: 900px) {

    .overlay {
      padding: 70px;
    }

    .cta-content {
      flex-direction: row;

      justify-content: space-between;
      align-items: center;

      text-align: left;

      max-width: 1200px;
    }

    .cta-text h2 {
    
      font-size: 30px;
    }

    .cta-text p {
      font-size: 17px;
    }

    .cta-button {
      font-size: 17 px;
        margin-right: 50px;
      padding: 12px 39px;
    }

  }
</style>